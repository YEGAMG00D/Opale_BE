name: Deploy Spring Boot to EC2 with Docker

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 레포 코드 체크아웃
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ EC2로 프로젝트 전체 업로드
      - name: Upload project to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "./"
          target: "/home/ec2-user/opale_be"
          overwrite: true   # 기존 폴더 덮어쓰기
          rm: true          # EC2쪽에 남은 예전 파일 정리

      # 3️⃣ EC2 접속해서 Docker 빌드 + 컨테이너 재실행
      - name: SSH into EC2 and deploy
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          timeout: 1500s   # 🔥 여기만 넉넉하게 늘림 (25분까지 허용)
          script: |
            set -e
            cd /home/ec2-user/opale_be

            echo "🔨 Docker 이미지 빌드 중..."
            docker build -t opale-be .

            echo "🧹 기존 백엔드 컨테이너 종료 및 삭제..."
            docker rm -f opale-be || true

            echo "🚀 새 컨테이너 실행"
            docker run -d \
              --name opale-be \
              --network opale-net \
              -p 8080:8080 \
              -v /home/ec2-user/resources/application.yml:/app/application.yml \
              -e SPRING_CONFIG_LOCATION=file:/app/application.yml \
              opale-be

            echo "🎉 Deployment completed successfully!"

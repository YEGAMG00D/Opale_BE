name: Deploy Spring Boot to EC2 with Docker

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker Image
        run: |
          docker build -t opale-be:latest .

      - name: Save Docker image as tar
        run: |
          docker save opale-be:latest -o opale-be.tar

      - name: Upload tar to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "opale-be.tar"
          target: "/home/ec2-user/opale_be/opale-be.tar"

      - name: SSH into EC2 and deploy
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          timeout: 600s
          script: |
            set -e
            cd /home/ec2-user/opale_be

            echo "🧹 기존 컨테이너 종료"
            docker rm -f opale-be || true

            echo "🧹 기존 이미지 삭제"
            docker rmi opale-be:latest || true

            echo "📦 tar → Docker 이미지 로드"
            docker load -i opale-be.tar

            echo "🚀 새 컨테이너 실행"
            docker run -d \
              --name opale-be \
              --network opale-net \
              -p 8080:8080 \
              -v /home/ec2-user/resources/application.yml:/config/application.yml \
              opale-be:latest

            echo "🎉 Deployment completed!"

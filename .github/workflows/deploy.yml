name: Deploy Spring Boot to EC2 (DEBUG MODE)

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug — root directory
        run: |
          echo "📌 현재 위치(pwd):"
          pwd
          echo "📌 현재 디렉토리 목록:"
          ls -al

      - name: Build JAR
        run: |
          echo "===================="
          echo "📌 GRADLE BUILD START"
          echo "===================="
          
          pwd
          ls -al
          
          chmod +x gradlew
          ./gradlew clean build -x test

          echo "===================="
          echo "📁 build/libs 디렉토리 목록"
          echo "===================="
          ls -al build/libs

          echo "===================="
          echo "📌 실행 가능한 bootJar만 app.jar로 복사"
          echo "===================="
          cp build/libs/*SNAPSHOT.jar build/libs/app.jar

          echo "📁 build/libs 최종 상태"
          ls -al build/libs

      - name: Debug — jar upload source
        run: |
          echo "📁 Actions 내부 JAR 경로 확인"
          ls -al build/libs

      - name: Upload ONLY app.jar to EC2 using SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "build/libs/app.jar"
          target: "/home/ec2-user/opale_be/jar/app.jar"
          overwrite: true

      - name: Deploy on EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "📌 EC2 접속 성공"

            echo "📁 JAR 업로드 경로 확인"
            ls -al /home/ec2-user/opale_be/jar/

            docker network create opale-net || true
            docker rm -f opale-be || true

            cd /home/ec2-user/opale_be
            docker build -t opale-be .

            docker run -d \
              --name opale-be \
              --network opale-net \
              -p 8080:8080 \
              -v /home/ec2-user/resources/application.yml:/app/application.yml \
              -e SPRING_CONFIG_LOCATION=file:/app/application.yml \
              opale-be

            docker ps -a
            docker logs opale-be || echo "❌ 로그 없음"

      - name: Finished
        run: echo "🚀 모든 작업 완료 (DEBUG MODE)"

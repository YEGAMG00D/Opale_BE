name: Deploy Spring Boot to EC2 with Docker

on:
  push:
    branches: [ main ]  # main에 머지/푸시될 때만 배포

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # 1) 레포 코드 체크아웃
      - name: Checkout code
        uses: actions/checkout@v4

      # 2) 현재 레포 전체를 EC2로 업로드 (private repo여도 OK)
      - name: Upload project to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "./"
          target: "/home/ec2-user/opale_be"
          overwrite: true
          rm: true

      # 3) EC2에 접속해 도커 빌드/실행
      - name: SSH into EC2 and deploy
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            cd /home/ec2-user/opale_be

            # 네트워크 준비 (이미 있으면 무시)
            docker network create opale-net || true
            docker network connect opale-net mysql-opale || true

            # 도커 이미지 빌드
            docker build -t opale-be .

            # 기존 컨테이너 정리 후 재실행
            docker rm -f opale-be || true
            docker run -d --name opale-be --network opale-net -p 8080:8080 opale-be

            echo "🎉 Deployment completed!"
